[pytest]
testpaths = tests
pythonpath = app
addopts = --cov=src --cov=api --cov-report=term-missing

# Config da cobertura (antes em .coveragerc). O pytest-cov lê estas seções aqui.
[coverage:run]
source = src,api

[coverage:report]
show_missing = True
